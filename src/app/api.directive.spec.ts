import { ApiDirective } from './api.directive';

describe('ApiDirective', () => {
  it('should create an instance', () => {
    const directive = new ApiDirective();
    expect(directive).toBeTruthy();
  });
});
